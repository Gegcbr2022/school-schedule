# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Доступи R2 — лишаються локально, у git не потрапляють
.r2.env

# Телефони вчителів — генеруються локально, у публічну збірку не їдуть
src/data/contacts.ts

# Вивантаження зі шкільного журналу — персональні дані, лише локально
учителя.json

# Локальні налаштування Claude Code (конфіг запуску dev-сервера)
.claude/

# Пак збирається зі `src/data/seed` під час збірки — у git його місця немає.
public/data/school.json

# Зібраний веб для iOS-оболонки.
ios/Web/*
!ios/Web/.gitignore

# Артефакти релізу.
build/

# Знімки — робочі файли, у git їм місця немає.
ios/screenshots/*.png
